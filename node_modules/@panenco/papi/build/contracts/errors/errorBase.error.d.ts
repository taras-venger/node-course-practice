import { StatusCode } from '..';
import { HttpError } from 'routing-controllers';
/**
 * Base for API Error
 *
 * @category Error
 */
export declare class ErrorBase extends HttpError {
    code: number;
    reason: string;
    message: string;
    payload?: object;
    constructor(code: StatusCode, reason: string, message: string, payload?: object);
}
