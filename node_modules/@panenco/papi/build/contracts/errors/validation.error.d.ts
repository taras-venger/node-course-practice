import { BadRequest } from './badRequest.error';
/**
 * Http error with status code 400
 *
 * Returns a JSON error map of input validation errors
 *
 * @category Error
 */
export declare class ValidationError<T> extends BadRequest {
    errors: {
        [K in keyof T]?: string | string[] | object;
    };
    constructor(errors: {
        [K in keyof T]?: string | string[] | object;
    });
}
