import { ErrorBase } from './errorBase.error';
/**
 * Http error with status code 403
 *
 * @category Error
 */
export declare class Forbidden extends ErrorBase {
    constructor(reason: string, message: string, payload?: object);
}
