import { ErrorBase } from './errorBase.error';
/**
 * Http error with status code 409 (duplicates)
 *
 * @category Error
 */
export declare class Conflict extends ErrorBase {
    constructor(reason: string, message: string, payload?: object);
}
