import { ErrorBase } from './errorBase.error';
/**
 * Http error with status code 400
 *
 * @category Error
 */
export declare class BadRequest extends ErrorBase {
    constructor(reason: string, message: string, payload?: object);
}
