import { ErrorBase } from '..';
/**
 * Output DTO for HTTP Errors
 *
 * @category Contract
 */
export declare class ErrorRepresentation {
    message: string;
    reason: string;
    constructor(error?: ErrorBase);
}
